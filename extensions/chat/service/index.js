var a=globalThis.__syncHost__;if(a===void 0)throw new Error("This module was loaded outside Sync, or outside a handler's isolate.");var d=async(t,e)=>JSON.parse(a(t,JSON.stringify(e??{})));var i={order:t=>d("work.order",t)};function h(){return{"chat.delegate":l}}async function l(t){let e=r(t,"title");if(e==="")throw new Error("Call this with `title` set: it is what the conversation is called in the list a person reads, and nothing but you knows what this piece of work should be called there.");let n=r(t,"prompt");if(n==="")throw new Error("Call this with `prompt` set to the work itself. The agent you are delegating to reads that and nothing else of this conversation.");let s=r(t,"agent");if(s==="")throw new Error("Call this with `agent` set to the agent to raise, as Sync names them \u2014 `claude`, `codex`, `gemini`. Name the one you are, unless the work asks for another.");let o=r(t,"parent");return{work:await i.order({kind:"agent.session",agent:s,title:e,prompt:{text:c(n,o)},onInterrupted:"continue",...o===""?{}:{parent:o}}),title:e,agent:s,next:u(o)}}function c(t,e){return e===""?t:`${t}

---

If this needs work delegated of its own, call \`chat.delegate\` with \`parent\` set to \`${e}\` \u2014 the conversation this one came out of, not this one. A chain is two conversations deep, so what you delegate stands beside this rather than under it, and waits for this to finish.`}function u(t){return`The work is ordered. ${t===""?"It stands on its own rather than under a conversation, because no parent was named.":"It stands under the conversation you named, and waits if something else is already running there."} End your turn now: whatever that agent says last comes back here as an ordinary message once you are free, and there is nothing to poll and no way to hurry it.`}function r(t,e){if(typeof t!="object"||t===null)return"";let n=t[e];return typeof n=="string"?n.trim():""}export{h as default};
