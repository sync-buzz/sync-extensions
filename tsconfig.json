{
  // What an extension is allowed to name.
  //
  // `@sync/extension-api` resolves to the declarations Sync published, and
  // there is nothing else to resolve: an extension has no access to Sync's
  // source, so an import that reaches past the contract does not type-check
  // rather than being caught by review.
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "paths": {
      "@sync/extension-api": ["./vendor/sync-extension-api.d.ts"]
    }
  },
  "include": ["extensions/*/src/**/*.ts", "extensions/*/src/**/*.tsx"]
}
