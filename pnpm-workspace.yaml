# esbuild fetches a platform binary in a postinstall script. It is the bundler
# this repository builds extensions with, so the script is what makes the
# toolchain work at all rather than an optional convenience.
allowBuilds:
  esbuild: true
